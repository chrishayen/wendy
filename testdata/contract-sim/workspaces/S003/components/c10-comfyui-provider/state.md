# Local State: C10 ComfyUI Provider

Initial S003 provider state:

- Provider health: `healthy`.
- Capability: `cap_image_generate_gpu`.
- Backend: ComfyUI.
- Provider-local content ref after invocation: `pcr_s003_0001`.

Provider content refs are opaque runner-facing handoff tokens. They are not filesystem paths, backend queue IDs, or durable artifact IDs.
